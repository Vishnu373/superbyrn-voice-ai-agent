FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN python -m livekit.plugins.turn_detector.download_model

COPY . .

CMD ["python", "app/agent_orchestrator.py", "start"]
